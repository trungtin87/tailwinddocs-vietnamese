# .github/workflows/deploy.yml

name: 🚀 Deploy Next.js Static Site to GitHub Pages

on:
  # Kích hoạt khi có push vào nhánh chính (thay 'main' nếu cần)
  push:
    branches:
      - main
  # Cho phép chạy thủ công
  workflow_dispatch:

# Quyền cần thiết để action peaceiris/actions-gh-pages có thể push lên repo
permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      # --- Cấu hình Môi trường ---
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Nên dùng phiên bản LTS ổn định
          cache: 'npm' # Kích hoạt cache để tăng tốc độ cài đặt

      - name: Install dependencies
        run: npm install

      # --- BƯỚC BUILD VÀ EXPORT ---
      - name: Build Next.js
        run: npm run build

      # --- BƯỚC DEPLOY ---
      - name: Deploy to GitHub Pages 🚀
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out
          publish_branch: gh-pages
          keep_files: false
          allow_empty_commit: true
          # Add .nojekyll file
          cname: ''