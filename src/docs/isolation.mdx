import { ApiTable } from "@/components/api-table.tsx";
import { ResponsiveDesign } from "@/components/content.tsx";

export const title = "isolation";
export const description =
  "Các utility để kiểm soát việc một phần tử có nên tạo stacking context mới một cách rõ ràng hay không.";

<ApiTable
  rows={[
    ["isolate", "isolation: isolate;"],
    ["isolation-auto", "isolation: auto;"],
  ]}
/>

## Ví dụ

### Ví dụ cơ bản

Sử dụng các utility `isolate` và `isolation-auto` để kiểm soát việc một phần tử có nên tạo stacking context mới một cách rõ ràng hay không:

```html
<!-- [!code classes:isolate] -->
<div class="isolate ...">
  <!-- ... -->
</div>
```

### Thiết kế responsive

<ResponsiveDesign property="isolation" defaultClass="isolate" featuredClass="isolation-auto" />
